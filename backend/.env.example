# Auto-generated by tools/generate_env_example.py
# Names only. DO NOT put secrets in this file.

## General
# General environment switches.
ENV=

## Database (Postgres)
# Backend database connection.
# Runtime API traffic uses DB_APP_* (least-privilege DML role).
# Alembic migrations use DB_MIGRATOR_* (DDL-capable role).
# SSL is mandatory; set DB_SSLMODE=require unless your provider says otherwise.
DB_HOST=
DB_NAME=
DB_PORT=
DB_SSLMODE=
DB_APP_USER=
DB_APP_PASSWORD=
DB_MIGRATOR_USER=
DB_MIGRATOR_PASSWORD=

## CORS
# Comma-separated list of allowed origins for the backend.
CORS_ORIGINS=

## Auth / JWT
# Backend authentication settings.
ACCESS_TOKEN_EXPIRE_MINUTES=
JWT_ALGORITHM=
JWT_SECRET=

## Refresh tokens
# Refresh token / cookie settings.
REFRESH_COOKIE_DOMAIN=
REFRESH_COOKIE_NAME=
REFRESH_COOKIE_PATH=
REFRESH_COOKIE_SAMESITE=
REFRESH_COOKIE_SECURE=
REFRESH_TOKEN_EXPIRE_HOURS=

## Email (provider selection)
# EMAIL_PROVIDER defaults to resend when unset. smtp is a legacy alias for gmail.
EMAIL_PROVIDER=

## Email (From address)
# FROM_EMAIL is used only for providers: ses, resend. Do not use it for gmail/smtp.
FROM_EMAIL=

## Email (Resend)
# Used when EMAIL_PROVIDER=resend.
RESEND_API_KEY=

## Email (Gmail/SMTP)
# Used when EMAIL_PROVIDER=gmail (or legacy smtp). From address is SMTP_FROM_EMAIL.
SMTP_FROM_EMAIL=
SMTP_HOST=
SMTP_PASSWORD=
SMTP_PORT=
SMTP_USERNAME=
SMTP_USE_SSL=
SMTP_USE_TLS=

## Email verification
# Email verification link settings.
EMAIL_VERIFY_TOKEN_EXPIRE_HOURS=
FRONTEND_BASE_URL=

## Server URLs
# Used to build server-side links in some flows.
PUBLIC_BASE_URL=

## Rate limiting
# SlowAPI rate limiting toggle.
ENABLE_RATE_LIMITING=

## Uploads
# Upload guardrails (max file size etc.).
MAX_PENDING_UPLOADS_PER_JOB=
MAX_UPLOAD_BYTES=

## AWS (S3 uploads)
# S3 bucket + prefix for document uploads.
AWS_REGION=
S3_BUCKET_NAME=
S3_PREFIX=

## Internal callbacks
# Shared secret for internal Lambda callbacks.
DOC_SCAN_SHARED_SECRET=
