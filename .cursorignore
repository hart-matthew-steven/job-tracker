# .cursorignore
# Controls Cursor indexing/search context. Goal: reduce noise, control cost, and avoid accidental context bloat.

# --- Dependencies / build outputs ---
node_modules/
dist/
build/
coverage/
.cache/

# --- Python caches / envs ---
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
ruff_cache/

# --- Secrets / env files ---
.env
.env.*
!.env.example

# --- Logs & run artifacts (excluded from indexing by default) ---
logs/

# --- One-off scripts ---
# NOTE: Cursor ignore rules do not reliably support negation in all environments.
# Keep this folder indexed so we can edit committed scripts like reset_dev_db.py.

# --- OS / editor noise ---
.DS_Store
.vscode/
.idea/